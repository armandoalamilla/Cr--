PROGRAM algo:

VAR x,y,z,numero,iterador:INT;

MAIN
{
  iterador = 0;
  READ(x);
  y=1;
  z=0;
  numero=10;
  PRINT('SERIE TIO FIBO');
  WHILE(iterador < numero)
  {
    PRINT(x);
    z = x + y;
    x = y;
    y = z;
    iterador = iterador + 1;
  };

}

END
