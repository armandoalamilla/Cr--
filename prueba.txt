PROGRAM algo:

VAR x,y,z,numero,iterador:INT;
VAR flota:FLOAT;
VAR lala:CHAR;

MAIN
{
  iterador = 0;
  READ(x);
  y=1;
  z=0;
  numero=10;
  PRINT('SERIE TIO FIBO');
  WHILE(iterador < numero)
  {
    PRINT(x);
    z = x + y;
    x = y;
    y = z;
    iterador = iterador + 1;
  };

  READ(lala);
  PRINT(lala);
}

END
